# -----------------------------------------------------------------------------
# FRONTEND ENVIRONMENT VARIABLES
# Note: Variables must start with VITE_ to be readable by your React components.
# -----------------------------------------------------------------------------

# Your local Node.js backend API URL (Change this when you deploy to Render/Vercel)
VITE_API_URL=http://localhost:5000/api

# Cloudinary Cloud Name (Useful if you need to use Cloudinary's React SDK for image resizing on the fly)
VITE_CLOUDINARY_CLOUD_NAME=dati8bges

# Store Contact Info (Optional: Useful if you want to reference it globally in components)
VITE_STORE_WHATSAPP=+8801626042411